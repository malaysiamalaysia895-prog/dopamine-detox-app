name: dopamine_detox
description: A new Flutter project for Dopamine Detox.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  android_intent_plus: ^5.3.1
  connectivity_plus: ^6.1.5
  device_apps: ^2.2.0
  flutter_overlay_window: ^0.4.5
  in_app_purchase: ^3.2.0
  pedometer: ^3.0.0
  permission_handler: ^11.3.1
  shared_preferences: ^2.3.3
  smooth_page_indicator: ^1.2.1
  lottie: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
