name: Build Android APK

on:
  push:
    branches: [ "Merge-app" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      - name: Auto-Find Project, Fix Android, and Build
        run: |
          echo "Dhoondh raha hoon pubspec.yaml kahan hai..."
          PUBSPEC=$(find . -name "pubspec.yaml" | head -n 1)
          DIR=$(dirname "$PUBSPEC")
          echo "Folder mil gaya yahan: $DIR"
          cd "$DIR"
          
          # Pehle packages download karein
          flutter pub get
          
          # YEH NAYI COMMAND HAI JO ERROR FIX KAREGI
          echo "Replit ki missing Android files ko naya bana raha hoon..."
          flutter create .
          
          # Ab final APK banayega
          flutter build apk --release
          
          # APK ko bahar nikalne ke liye
          mkdir -p $GITHUB_WORKSPACE/output
          find build -name "*.apk" -exec cp {} $GITHUB_WORKSPACE/output/ \;
        
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Mera-Game-APK
          path: output/*.apk
