name: Flutter Build APK & AAB

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: 'stable'

      - name: Fix Android Folder aur Kotlin Version
        run: |
          rm -rf android
          flutter create --platforms android --project-name dopamine_detox .
          sed -i 's/ext.kotlin_version = .*/ext.kotlin_version = "1.9.22"/g' android/build.gradle || true
          sed -i 's/version "1.7.10"/version "1.9.22"/g' android/settings.gradle || true
          sed -i 's/version "1.8.22"/version "1.9.22"/g' android/settings.gradle || true
          sed -i 's/compileSdk = flutter.compileSdkVersion/compileSdk = 34/g' android/app/build.gradle || true
      
      - name: Install Packages
        run: flutter pub get

      - name: Build APK (Mobile ke liye)
        run: flutter build apk --release

      - name: Build AAB (Play Store ke liye)
        run: flutter build appbundle --release

      - name: Upload APK File
        uses: actions/upload-artifact@v4
        with:
          name: dopamine-detox-apk
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Upload AAB File
        uses: actions/upload-artifact@v4
        with:
          name: dopamine-detox-aab
          path: build/app/outputs/bundle/release/app-release.aab
